# osv-scanner ignore config. Previous [[IgnoredVulns]] entries were dropped once
# their vulnerabilities aged out of the dependency tree — osv-scanner fails on
# unused ignores. Re-add [[IgnoredVulns]] blocks only for advisories osv-scanner
# actively reports that have no available fix.
